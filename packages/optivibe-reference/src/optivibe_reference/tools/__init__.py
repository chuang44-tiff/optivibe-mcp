"""optivibe_reference.tools — dispatchable reference tools.

Each tool module exposes a ``TOOL_SPECS`` tuple of ``ToolSpec``s the
``server.load_manifest`` importlib loop collects into the dispatch manifest.
"""
